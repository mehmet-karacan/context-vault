[alembic]
# Path to migration scripts, relative to this file (services/backend/).
script_location = alembic
version_locations = %(here)s/alembic/versions_v3

# Template used to generate migration file names.
file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d_%%(rev)s_%%(slug)s

# Timezone used when rendering the date/time in new revision file names.
timezone = UTC

# Max length of the "slug" appended to migration filenames.
truncate_slug_length = 40

# sqlalchemy.url is intentionally left blank here. alembic/env.py overrides
# it at runtime from src.migration_settings.MigrationSettings.DATABASE_URL.
# Provider, Redis and object-storage settings are deliberately not loaded.
sqlalchemy.url =

[post_write_hooks]

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
