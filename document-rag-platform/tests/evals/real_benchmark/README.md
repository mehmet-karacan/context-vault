# Approved real-provider benchmark tier

This tier is release-gating and must run only with an approved private-pack
manifest, explicit `--approval-id`, provider credentials and compatible data
classification. Reports record provider/model/profile/prompt/config hashes.
Golden answers and expected sources are never included in provider requests.
