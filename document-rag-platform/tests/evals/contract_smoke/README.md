# Contract-smoke tier

This tier may use fake retrievers and answerers only to validate schemas,
orchestration and metric arithmetic. Reports are named `contract-smoke` and set
`quality_claim=false`; they cannot satisfy a release-quality gate.
